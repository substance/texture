
module.exports = {

  name: 'flatten-sections',
  type: 'section',

  import: function(doc) {
    /*
      This should traverse the body and turn sections into
      a flattened sequence with headings
    */
  },

  export: function(doc) {
    /*
      This should traverse the body and turn headings into sections.
    */
  },

};
